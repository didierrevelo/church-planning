{
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "npm config set cache /tmp/npm-cache && npm install --prefer-offline --no-audit --no-fund && npx prisma generate && npm run build"
  },
  "deploy": {
    "startCommand": "npx prisma db push && npm start",
    "healthcheckPath": "/health",
    "healthcheckTimeout": 300,
    "restartPolicyType": "on_failure"
  }
}
