{
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "npm config set cache /tmp/npm-cache && npm install --include=dev --prefer-offline --no-audit --no-fund && npx prisma generate && npx prisma db push && npm run build"
  },
  "deploy": {
    "startCommand": "npm start",
    "healthcheckPath": "/health",
    "healthcheckTimeout": 300,
    "restartPolicyType": "on_failure"
  }
}
